version: "3.9"

services:
  xatext:
    build: .
    container_name: xatext-service
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
    command: uvicorn src.app.main:app --host 0.0.0.0 --port 8000 --reload
